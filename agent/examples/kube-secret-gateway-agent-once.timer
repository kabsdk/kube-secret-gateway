[Unit]
Description=Run kube-secret-gateway-agent periodically

[Timer]
OnBootSec=2min
OnUnitActiveSec=15min
# Spreads the load of many hosts that were installed together.
RandomizedDelaySec=2min
# Catches up after the host was off.
Persistent=true

[Install]
WantedBy=timers.target
